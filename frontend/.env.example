REACT_APP_API_URL=/api
REACT_APP_RECAPTCHA_SITE_KEY=change-me


# Solana RPC is server-side only — set SOLANA_RPC_URLS on the backend,
# never a REACT_APP_* var, or the key ends up in the public bundle.
